backend:
  name: github
  repo: YOUR_GITHUB_ID/rice_dog-site
  branch: main

media_folder: "uploads"
public_folder: "/uploads"

collections:
  - name: "content"
    label: "홈페이지 내용"
    files:
      - file: "content.json"
        label: "전체 내용 수정"
        name: "content"
        fields:
          - { label: "Tagline", name: "tagline", widget: "string" }
          - { label: "소개글", name: "about", widget: "text" }
